    # .streamlit/secrets.toml

    # --- Credenciais de Login (Para acessar o sistema) ---
    [login]
    username = "admin" # <-- Chave corrigida (sem espaços)
    password = "admin123" # <-- Chave corrigida (sem espaços)

    # Exemplo de como adicionar outro usuário após autorização:
    # usuario2_username = "outro_usuario"
    # usuario2_password = "senha_do_outro"

    # --- Configurações de E-mail para Solicitações de Acesso ---
    [smtp]
    sender_email = "seu_email_aqui@gmail.com" # <-- Chave corrigida (sem espaços)
    sender_password = "sua_senha_de_app_aqui" # <-- Chave corrigida (sem espaços)
    smtp_server = "smtp.gmail.com" # <-- Chave corrigida (sem espaços)
    smtp_port = 587              # <-- Chave corrigida (sem espaços)
    recipient_email = "dayanesancoe@gmail.com" # <-- Chave corrigida (sem espaços)
