    # .streamlit/secrets.toml

    # --- Credenciais de Login (Para acessar o sistema) ---
    [login]
    username = "admin"
    password = "admin123" # <-- SUBSTITUA POR UMA SENHA FORTE E REAL PARA O ADMIN

    # Exemplo de como adicionar outro usuário após autorização:
    # usuario2_username = "outro_usuario"
    # usuario2_password = "senha_do_outro"

    # --- Configurações de E-mail para Solicitações de Acesso ---
    [smtp]
    sender_email = "seu_email_aqui@gmail.com" # <-- SUBSTITUA PELO SEU E-MAIL REMETENTE REAL
    sender_password = "sua_senha_de_app_aqui" # <-- SUBSTITUA PELA SENHA DE APP DO SEU E-MAIL REMETENTE
    smtp_server = "smtp.gmail.com" # Servidor SMTP para Gmail (se usar outro, mude aqui)
    smtp_port = 587              # Porta padrão para TLS (se usar outro, mude aqui)
    recipient_email = "dayanesancoe@gmail.com" # <-- SEU E-MAIL PARA RECEBER AS SOLICITAÇÕES
