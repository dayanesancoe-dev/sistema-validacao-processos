    # .streamlit/secrets.toml

    # --- Credenciais do Administrador Inicial (para o primeiro login) ---
    # Este usuário será usado para acessar o sistema e criar outros usuários na aba "Gerenciar Usuários".
    # Após o primeiro login, você pode criar outros usuários diretamente no app.
      
        # .streamlit/secrets.toml

    # --- Credenciais do Administrador Inicial (para o primeiro login) ---
    [admin_user]
    username = "admin"
    password = "SUA_SENHA_FORTE_AQUI" # <-- VERIFIQUE ESTA SENHA!
